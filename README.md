# JavaScript Singly Linked List

## Methods

push(value)

- Create new list tail
  pop()
- Remove list tail
  unshift(value)
  shift()
  get(index)
  set(index, value)
  print()
  insert(index, value)
  remove(index)
